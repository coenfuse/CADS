#pragma once
/*
* Direct List that promises O(1) access to every node.
* It does so by maintaining an array that contains addresses of every node in the list.
* So, a list and an array will run in parallel. List is of Ring type.
*/
